# Rust
/target/

# staleguard cache
.staleguard/
.staleguard-cache/

# fastembed model cache (downloaded ONNX models, hundreds of MB)
.fastembed_cache/

# Env / secrets
.env
.env.*

# Layer-3 holdout sample — third-party OSS snippets, not for redistribution.
# Generate locally with tools/gen_holdout_sample.py (see src/judge.rs).
nli_holdout_sample.jsonl

# OS / editor
.DS_Store
.idea/
.vscode/
